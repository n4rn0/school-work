choice = input("Enter 1 for kg and 2 for lb ")
answer = 0
kg = 0
lb = 0
if choice == "1":
  kg = float(input("Enter Weight in kg "))
  lb = (kg * 2.2046)
  print(lb)
elif choice == "2":
  lb = float(input("Enter Weight in lb "))
  kg = (lb / 2.2046)
  print(kg)
